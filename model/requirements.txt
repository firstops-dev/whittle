torch>=2.6
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
tiktoken==0.8.0
llmlingua==0.2.2
huggingface_hub==0.27.0
# router embedding + complexity signals (mmBERT-embed-32k-2d-matryoshka, built with
# sentence-transformers 5.x). Verified on a fresh venv: domain labels, bank-score
# separation, and complexity margins all correct; the 3.x version warning is gone.
sentence-transformers==5.6.0
transformers>=4.48.0          # router domain signal: ModernBertForSequenceClassification (added in 4.48)
